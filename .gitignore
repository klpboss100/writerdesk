# === 비공개 / 로컬 전용 — 공개 GitHub 저장소에 올리지 않음 ===

# API 키 포함 — 절대 커밋 금지
wd_config.json
# 로컬 세션 상태
wd_session.json

# Windows 실행 스크립트 (로컬 전용)
writerdesk.bat
writerdesk_start.vbs
웹동기화.bat

# Claude Code 로컬 설정/문서
.claude/
CLAUDE.md
CLAUDE.md.docx
claude_code.bat
claude_code_start.vbs

# 설치 파일 (node 등)
*.msi

# 파이썬 캐시
__pycache__/
*.pyc
